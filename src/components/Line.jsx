const Line = () => {
  return (
    <div className="site-bottom-smoke" aria-hidden="true">
      {/* thin blurred line overlay for a soft edge */}
      <span className="smoke-line" />
    </div>
  );
};

export default Line;
